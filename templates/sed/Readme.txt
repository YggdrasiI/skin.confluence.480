This sed scripts can be used as boilerplate
to transform the xml files of a skin into 
a template version.

Usage: 
./runSubstitutions.sh *.sed

Note: 
Do not run accidently the sed script accidentally twice on the same file.
